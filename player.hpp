#pragma once

#include <string>
#include <cstddef>

enum class PlayerMarker{
    NOUGHT, CROSS
};

struct Player{
    
    std::string name;
    
    //AI / Human flag
};

class PlayerManager{

    public:

    private:

};